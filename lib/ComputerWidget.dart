import 'package:flutter/material.dart';

class ComputerWidget extends StatelessWidget {
  const ComputerWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(children: [
      Container(
        child: Text("ComputerWidget"),
      )
    ],);
  }
}